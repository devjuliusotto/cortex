{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for Cortex",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default"
  ]
}
